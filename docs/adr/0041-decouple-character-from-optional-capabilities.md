# 角色身份不依赖可选展示与语音能力

角色只以稳定身份、显示名称和角色描述作为参与对话的必要条件；Live2D 展示、GPT-SoVITS 语音和头像均作为独立可选能力。角色目录扫描不得因 Live2D 资源缺失、模型规范化失败或语音资源不完整而排除角色，因为这会把角色的对话身份错误地绑定到当前尚未取得或已经损坏的表现资源；已配置但加载失败的 Live2D 模型可以退化为无模型展示，但必须记录错误日志，不能与正常未配置混为同一状态。
