# 在依赖包之间显式续接主观状态身份

后作世界书可以显式沿用其依赖闭包中已有的 `relation_type_key` 或 `thought_thread_key`，让各包内 entry UUID 仍保持唯一的同时，把同一关系方面或观点脉络组合为跨季度时间序列；续接不能由正文相似度推断。选择前作根包时只使用前作状态，选择后作根包时由依赖方向和 `visible_from` 解析共享 Key 的后继状态，后作新状态开始后接替仍开放的前作状态，历史查询保留两者；同一层级出现无法判定先后的冲突状态会使该根包检索不可用。具体审核产物如何声明续接在实现对应依赖包前另行确定。
