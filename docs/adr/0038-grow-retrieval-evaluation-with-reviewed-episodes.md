# 随已审核分集增量扩展检索验收集

世界书运行时接入同时建立版本管理内的检索验收格式、fake embedding 快速测试、真实 `multilingual-e5-small` 发布前测试和隐藏工具循环测试；当前只为已经进入正式世界书包的少量分集加入人工确认案例，不等待 13 集全部完成，也不据小样本冻结最终阈值。每发布一集就补充首次可见知识、未来泄漏、角色视角、关系变化、Lore 专名和低相关负例，完成 13 集后再统一复核约 30–50 个案例并冻结第一版阈值；E5 检索统一使用 query/passage 角色前缀并通过索引 fingerprint 触发重建。
