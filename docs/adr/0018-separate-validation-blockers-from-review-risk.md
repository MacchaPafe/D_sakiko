# Separate validation blockers from review risk

Every Character Thought may be reviewed, with the dataset viewer showing review status, explainable risk reasons, severity, and a sortable risk score. Structural validation errors block import, while semantic risk flags only prioritize optional review and never reject an otherwise valid record automatically; thresholds remain configurable.
