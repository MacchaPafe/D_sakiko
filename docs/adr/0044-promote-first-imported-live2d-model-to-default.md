# 将角色首次导入的 Live2D 模型设为默认模型

没有默认 Live2D 模型的角色首次导入模型时，导入器将自包含且完成规范化的模型写入 `live2D_model` 并把它作为角色默认模型；只有角色已经拥有默认模型时，后续导入才进入 `extra_model`。如果所有导入都进入 `extra_model`，无模型角色仍会在每个新对话中缺少默认展示，用户不得不逐个选择对话级模型；同时导入流程不得覆盖已有默认模型。
