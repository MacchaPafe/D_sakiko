# Allow parallel character thoughts about one event

A character may hold multiple simultaneous Character Thoughts about different aspects of the same Story Event. Stage 3 assigns updates to Thought Threads such as status, cause, or responsibility, and changes `valid_to` only when an update explicitly corrects or replaces an earlier thought in the same thread.
