# Backdate character thoughts only with explicit anchors

Character Thoughts become effective at their Evidence Time by default, even when dialogue vaguely claims they existed earlier. Stage 3 may backdate `valid_from` only when the subtitle supplies an explicit prior anchor that can be linked reliably; otherwise it preserves an earlier-than-current hint without guessing a timestamp.
