# 按世界书包依赖限定检索范围

官方世界书包按动画季度划分，共享世界观进入独立通用包；每条索引记录携带来源 `package_id`，未来运行时只检索对话根包及其依赖闭包，Lore 的全局语义也只在该集合内成立。所有已安装包可以统一进入索引，对话切换不触发重建；相比按品牌、`series_id` 或 `canon_branch` 推断范围，这要求显式维护依赖，却能隔离世界关系尚未确认的作品，并允许以后通过调整依赖表达新的官方信息。
