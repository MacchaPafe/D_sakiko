# Pilot the character-thought pipeline before switching runtime retrieval

Implementation first delivers Stage 2B extraction, Stage 3 linking and timeline projection, risk analysis, and dataset-viewer review for a small representative scene set. Qdrant import and runtime retrieval follow only after calibration, with feature flags supporting side-by-side comparison before objective Story Event injection is disabled by default.
