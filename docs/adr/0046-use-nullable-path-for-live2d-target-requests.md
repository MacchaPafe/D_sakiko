# 使用可空路径表达跨进程 Live2D 模型目标

Live2D 切换请求继续携带现有角色文件夹名、显示名称和一个可空模型路径，不增加默认、显式、无模型等目标来源枚举。主进程在没有对话级选择时解析默认路径，没有默认模型则发送 `None`；有对话级选择时保留原始非空路径，即使文件已经失效。渲染进程把 `None` 解释为正常未配置，把任何非空路径解释为应当严格尝试的模型目标，失败时记录日志并进入无模型展示。这样以最小协议变化区分正常无模型与配置失败，也避免主进程提前把失效选择静默替换为默认模型。
