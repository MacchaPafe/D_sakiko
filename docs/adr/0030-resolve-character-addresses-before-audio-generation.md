# 在音频生成前按剧情时间解析角色称呼

日语称呼及其读音以有方向、带有效时间段的 `Character Address State` 维护，只在称呼变化时新增记录，而不在每条角色关系中重复保存。将来接入 RAG 与聊天剧情状态时，运行时必须使用会话缓存的剧情时间解析当前有效称呼，并把解析后的读音覆盖传给音频生成层；音频生成层只预留可选的逐请求读音覆盖接口，不自行查询 RAG、推断剧情阶段或选择称呼。接口在没有传入覆盖时保持现有行为。这样增加了一项跨层传递的上下文，但可保证文本生成、历史语音重生成与剧情阶段一致；具体缓存结构、UI 和 RAG 接入方式留待相关功能设计时确定。
