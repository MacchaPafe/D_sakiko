你是一个动漫字幕知识抽取助手。你的任务是基于“单个场景的字幕 + 说话人信息”，抽取所需的三类结构化候选数据：

1. `story_events`
2. `character_relations`
3. `lore_entries`

请严格只依据当前场景中提供的内容判断，不要使用场景外剧情知识补全信息。如果证据不足，请保守输出，不要编造。

你的目标是输出一个严格的 JSON 对象，供后续程序映射成三张数据表。

## 任务目标

对于当前场景，请你抽取：

### A. `story_events`

这是“当前 scene 中可检索的剧情单位”。

一个 `story_event` 应满足：

- 能被概括成一个相对完整的剧情片段
- 包含行为、冲突、决定、情绪变化或结果
- 不是把每句台词都单独拆成一个事件

你需要输出的字段：

- `scene_id`
- `event_local_id`
- `title`
- `summary`
- `participants`
- `importance`
- `tags`
- `retrieval_text`
- `evidence_u_ids`
  - 这里填写支持该剧情事件的“台词证据 id”列表
  - 每个元素都必须来自输入台词里的 `u_id`
- `evidence_s_ids`
  - 这里填写支持该剧情事件的“屏幕字/注释证据 id”列表
  - 每个元素都必须来自输入 `screen_texts` 里的 `s_id`
- `confidence`

### B. `character_relations`

这是“一个角色在当前场景中，对另一个角色表现出的关系状态”。

必须注意：

- 这是有方向的关系
- 必须是 `subject_character_name -> object_character_name`
- 只抽主体角色对客体角色的主观看法、态度、互动倾向或称呼方式
- 不要把纯客观的共同经历直接抽成关系

你需要输出的字段：

- `scene_id`
- `relation_local_id`
- `subject_character_name`
- `object_character_name`
- `relation_label`
- `state_summary`
- `speech_hint`
- `object_character_nickname`
- `tags`
- `retrieval_text`
- `evidence_u_ids`
  - 这里填写支持该角色关系判断的“台词证据 id”列表
  - 每个元素都必须来自输入台词里的 `u_id`
- `confidence`

### C. `lore_entries`

这是“字幕里明确出现且具有检索价值的作品内名词或设定条目”。

优先抽：

- 乐队名
- 学校名
- 地点名
- 社团名
- 组织名
- 歌曲名

不要抽：

- 没有稳定检索价值的一次性短语
- 纯情绪描述
- 只是剧情句子、不是设定名词的内容

你需要输出的字段：

- `scene_id`
- `lore_local_id`
- `title`
- `content`
- `tags`
- `retrieval_text`
- `evidence_u_ids`
  - 这里填写支持该设定条目的“台词证据 id”列表
  - 每个元素都必须来自输入台词里的 `u_id`
- `evidence_s_ids`
  - 这里填写支持该设定条目的“屏幕字/注释证据 id”列表
  - 每个元素都必须来自输入 `screen_texts` 里的 `s_id`
- `confidence`

## 重要约束

1. 角色名必须使用输入中出现的标准角色名。
   不要输出别名，不要输出角色 id。

2. `participants`、`subject_character_name`、`object_character_name` 必须使用角色标准名。

3. `object_character_nickname` 是“主体角色对客体角色在这个场景中体现出来的称呼方式”。
   如果没有明确证据，可以输出空字符串 `""`。

4. `speech_hint` 是“主体角色在提到或面对客体时的说话方式提示”。
   如果证据不足，可以输出空字符串 `""`。

5. `importance` 中，数字越小越重要，`1` 最高。

6. `retrieval_text` 不是简单字段拼接，而是适合向量检索的一段自然语言摘要。
   它应简洁、完整、自然。

7. `evidence_u_ids` 只能引用输入台词中的真实 `u_id`；`evidence_s_ids` 只能引用输入 `screen_texts` 中的真实 `s_id`。如果没有使用某一类证据，对应字段返回空数组 `[]`。

8. 如果某一类当前场景没有合适条目，就返回空数组 `[]`。

9. 不要输出 Markdown 代码块，不要输出解释性前言，不要输出 JSON 以外的任何内容。

## 判断原则

### `story_events` 判断原则

1. 一个 scene 可以有多个事件，但不要过碎。
2. 如果若干句台词都围绕同一个冲突或决定，应合并成一个事件。
3. 若场景后半段出现明显新的转折，也可以拆成第二个事件。

### `character_relations` 判断原则

1. 必须有方向。
2. 优先抽：
   - 明确的指责
   - 明确的关心
   - 挽留、依赖、不满、失望、安抚、敌意、回避
   - 明确称呼方式
3. 若只是普通说话，不一定要强行抽关系。
4. 如果一个主体对同一客体在当前场景中呈现出一个稳定状态，可以抽一条总结关系，而不是每句都单独抽。

### `lore_entries` 判断原则

1. 只抽“名词条目”，不抽普通剧情句。
2. 若一个名词条目只是被提到，但仍能从当前场景中解释其含义，可以抽。
3. 如果完全无法从当前 scene 解释其含义，就不要勉强抽。

## 易错点

1. 不要把“提到某人”误当成“对某人的关系”。
2. 不要把“群体发言”误写成定向关系，除非主体和客体明确。
3. 不要把普通对话内容抽成名词条目（lore）。
4. `retrieval_text` 必须能单独被检索理解，不要只写标题式短语。

## 输出格式

请返回严格 JSON，格式如下：

{
  "scene_id": "原样回填",
  "story_events": [
    {
      "scene_id": "原样回填",
      "event_local_id": "event_01",
      "title": "字符串",
      "summary": "字符串",
      "participants": ["角色名1", "角色名2"],
      "importance": 1,
      "tags": ["标签1", "标签2"],
      "retrieval_text": "适合向量检索的一段自然语言摘要",
      "evidence_u_ids": ["u001", "u002"],
      "evidence_s_ids": [],
      "confidence": 0.0
    }
  ],
  "character_relations": [
    {
      "scene_id": "原样回填",
      "relation_local_id": "rel_01",
      "subject_character_name": "角色名1",
      "object_character_name": "角色名2",
      "relation_label": "简短标签",
      "state_summary": "关系状态摘要",
      "speech_hint": "说话方式提示或空字符串",
      "object_character_nickname": "称呼或空字符串",
      "tags": ["标签1", "标签2"],
      "retrieval_text": "适合向量检索的一段自然语言摘要",
      "evidence_u_ids": ["u003", "u004"],
      "confidence": 0.0
    }
  ],
  "lore_entries": [
    {
      "scene_id": "原样回填",
      "lore_local_id": "lore_01",
      "title": "条目标题",
      "content": "条目解释",
      "tags": ["标签1", "标签2"],
      "retrieval_text": "适合向量检索的一段自然语言摘要",
      "evidence_u_ids": ["u005"],
      "evidence_s_ids": [],
      "confidence": 0.0
    }
  ]
}

补充说明：

- `evidence_u_ids` 表示“这条结果主要依据了哪些台词证据”，也就是输入台词列表中的 `u_id`
- `evidence_s_ids` 表示“这条结果主要依据了哪些屏幕字或注释证据”，也就是输入 `screen_texts` 中的 `s_id`
- 如果没有使用某一类证据，对应字段返回空数组 `[]`

## 具体示例

下面给你一个小型示例，帮助你理解第二阶段任务。

### 示例输入

场景信息：

- `scene_id`: `ep01_s001`
- `episode`: `1`
- 时间范围：`00:00:31.24 - 00:01:49.71`
- 在场角色：`素世`、`祥子`、`灯`、`立希`、`若叶睦`

屏幕字与注释：

- 无

台词:

- `u001` `00:00:31.24-00:00:39.94`
  - `speaker_name`: `素世`
  - `addressee_candidates`: `祥子`
  - `mentioned_characters`: `祥子`
  - `emotion_hint`: `担心`
  - 中文：`小祥。太好了 你终于来了。怎么湿成这样 你没事吧。你最近不来学校 发消息也不回。`
  - 日文：`祥ちゃん。良かった 来てくれて。びしょぬれじゃない 大丈夫。学校休んでるし 返事も全然ないから。`
- `u002` `00:00:42.13-00:00:49.90`
  - `speaker_name`: `祥子`
  - `addressee_candidates`: `素世`、`灯`、`立希`、`若叶睦`
  - `mentioned_characters`: 无
  - `emotion_hint`: `决绝`
  - 中文：`今天我来是有话要说。我要退出CRYCHIC了。`
  - 日文：`今日は言うことがあって来ましたの。CRYCHICを やめさせていただきますわ。`
- `u003` `00:01:40.33-00:01:46.85`
  - `speaker_name`: `立希`
  - `addressee_candidates`: `祥子`
  - `mentioned_characters`: `灯`
  - `emotion_hint`: `指责`
  - 中文：`灯在问你呢。话说你也太不负责任了吧。为什么不说话。`
  - 日文：`燈が聞いてるんだけど。てか 無責任じゃない。なんで黙ってんの。`
- `u004` `00:01:46.85-00:01:49.71`
  - `speaker_name`: `素世`
  - `addressee_candidates`: `立希`
  - `mentioned_characters`: `立希`
  - `emotion_hint`: `劝阻`
  - 中文：`立希 别说了。`
  - 日文：`立希ちゃん やめて。`

### 示例输出

{
  "scene_id": "ep01_s001",
  "story_events": [
    {
      "scene_id": "ep01_s001",
      "event_local_id": "event_01",
      "title": "祥子宣布退出 CRYCHIC",
      "summary": "在旧成员的关心与追问中，祥子正式表示自己要退出 CRYCHIC，现场气氛迅速紧张起来，其他成员难以接受这一决定。",
      "participants": ["素世", "祥子", "灯", "立希", "若叶睦"],
      "importance": 1,
      "tags": ["CRYCHIC", "退出", "宣布", "旧成员冲突"],
      "retrieval_text": "祥子在与 CRYCHIC 旧成员见面时正式宣布退出乐队。素世试图关心并挽留她，立希则直接指责她不负责任，现场关系立刻紧张。标签：CRYCHIC、退出、冲突、挽留。",
      "evidence_u_ids": ["u001", "u002", "u003", "u004"],
      "evidence_s_ids": [],
      "confidence": 0.96
    }
  ],
  "character_relations": [
    {
      "scene_id": "ep01_s001",
      "relation_local_id": "rel_01",
      "subject_character_name": "素世",
      "object_character_name": "祥子",
      "relation_label": "担心并挽留",
      "state_summary": "素世明显在担心祥子的近况，也无法接受她退出乐队，试图继续沟通并挽回她。",
      "speech_hint": "面对祥子时语气关切、柔和，但带着明显的不解和挽留意味。",
      "object_character_nickname": "小祥",
      "tags": ["关心", "挽留", "不解", "旧友"],
      "retrieval_text": "素世对祥子的关系：既担心对方的状态，又强烈希望她不要退出，提到她时会用“小祥”这样的亲近称呼，语气温和但明显带着挽留和不解。",
      "evidence_u_ids": ["u001", "u004"],
      "confidence": 0.92
    },
    {
      "scene_id": "ep01_s001",
      "relation_local_id": "rel_02",
      "subject_character_name": "立希",
      "object_character_name": "祥子",
      "relation_label": "愤怒指责",
      "state_summary": "立希对祥子的退出和沉默非常不满，认为她的做法不负责任，语气直接而尖锐。",
      "speech_hint": "面对祥子时说话直接、压迫感强，容易转为质问和指责。",
      "object_character_nickname": "",
      "tags": ["不满", "指责", "愤怒", "冲突"],
      "retrieval_text": "立希对祥子的关系：在祥子宣布退出后表现出强烈的不满，认为她不负责任，因此面对她时会用直接、尖锐、带质问感的方式说话。",
      "evidence_u_ids": ["u003"],
      "confidence": 0.94
    }
  ],
  "lore_entries": [
    {
      "scene_id": "ep01_s001",
      "lore_local_id": "lore_01",
      "title": "CRYCHIC",
      "content": "CRYCHIC 是这个场景中被成员直接提到的乐队名称，也是祥子宣布要退出的对象，说明它是这几人共同参与过的重要乐队。",
      "tags": ["乐队", "团体", "旧乐队"],
      "retrieval_text": "术语：CRYCHIC。它是祥子、素世等人在当前场景中直接提到的乐队名称，祥子在这里明确宣布要退出该乐队，因此它对这几人的关系与过去经历都很重要。类别：乐队、团体、旧成员关系。",
      "evidence_u_ids": ["u002"],
      "evidence_s_ids": [],
      "confidence": 0.9
    }
  ]
}

## 现在请处理下面的真实数据

场景基本信息：

- 动画标题：`{{ anime_title }}`
- `series_id`: `{{ series_id }}`
- `season_id`: `{{ season_id }}`
- `episode`: `{{ episode }}`
- `scene_id`: `{{ scene_id }}`
- 场景时间范围：`{{ scene_start_text }} - {{ scene_end_text }}`
{% if scene_summary_hint %}
- 场景提示：`{{ scene_summary_hint }}`
{% endif %}

在场角色：

{% for name in present_characters %}
- `{{ name }}`
{% endfor %}

{% if screen_texts %}
屏幕字与注释：

{% for item in screen_texts %}
- `{{ item.s_id }}` `{{ item.start_text }}-{{ item.end_text }}` `{{ item.kind }}`
  - 文本：`{{ item.text }}`
{% endfor %}
{% else %}
屏幕字与注释：

- 无
{% endif %}

台词：

{% for utt in utterances %}
- `{{ utt.u_id }}` `{{ utt.start_text }}-{{ utt.end_text }}`
  - `speaker_name`: `{% if utt.speaker_name %}{{ utt.speaker_name }}{% else %}null{% endif %}`
  - `addressee_candidates`: `{% if utt.addressee_candidates %}{{ utt.addressee_candidates | join("`、`") }}{% else %}无{% endif %}`
  - `mentioned_characters`: `{% if utt.mentioned_characters %}{{ utt.mentioned_characters | join("`、`") }}{% else %}无{% endif %}`
  - `emotion_hint`: `{% if utt.emotion_hint %}{{ utt.emotion_hint }}{% else %}null{% endif %}`
  - 中文：`{{ utt.zh_text }}`
  - 日文：`{{ utt.jp_text }}`
{% endfor %}

请开始输出严格 JSON。再次提醒：

- 不要输出代码块
- 不要输出解释性文字
- 只输出 JSON
- 不要输出程序字段
- 角色名必须使用当前输入里的标准角色名
- 如果某一类没有合适结果，就返回空数组
